#include "main.h"

/**
 * _abs: function that prints the absolute value of a number
 * Designed by: Ajah John Nnaemeka
 */

int _abs(int n)
{
	if(n < 0)
	{
		n = -n;
	}
	else
	{
		n = n;
	}
	return (n);
}
